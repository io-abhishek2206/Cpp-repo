#include<iostream>
using namespace std;
int main()
{
    long int t;
    while(t--)
    {
        long long int a,b;
        cin>>a>>b;
        if(a!=b){
        long long diff=abs(a-b);
        if(diff)
    }
}